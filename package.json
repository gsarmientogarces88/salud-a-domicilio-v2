{
  "name": "salud-a-domicilio",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../mobile-provider && npm install",
    "dev:mobile": "cd mobile-provider && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
